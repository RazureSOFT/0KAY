// Package version carries the 0KAY platform release version.
// Bump together with the root manifest and the git tag when cutting a release.
package version

// Version is the current 0KAY release (semver, without a leading "v").
const Version = "0.1.0"
